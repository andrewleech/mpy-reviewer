{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/setup.sh",
            "description": "Ensure mpy-reviewer dependencies are installed"
          }
        ]
      }
    ]
  }
}
