# Existing dependencies
requests>=2.28.0
anthropic>=0.25.0

# RAG system dependencies
lancedb>=0.4.0
transformers>=4.36.0
# torch>=2.0.0  # See note below

# NOTE: For torch, choose one:
# GPU (larger, ~2GB): pip install torch
# CPU-only (smaller): pip install torch --index-url https://download.pytorch.org/whl/cpu
click>=8.0.0
tqdm>=4.65.0

# For handling embeddings
numpy>=1.24.0
pyarrow>=14.0.0

# Optional dependencies (install with pip install 'mpy-review-rag[<feature>]'):
# - Re-ranking: sentence-transformers>=2.2.0
# - Codebase context: codanna-cli>=0.1.0
# - All optional features: pip install 'mpy-review-rag[all]'
