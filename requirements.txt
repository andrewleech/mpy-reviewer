# Core dependencies
requests>=2.28.0
anthropic>=0.25.0

# RAG system core
sqlite-vec>=0.1.6
transformers>=4.36.0
# torch>=2.0.0  # See note below
click>=8.0.0
tqdm>=4.65.0
numpy>=1.24.0

# Quality features (cross-encoder re-ranking)
sentence-transformers>=2.2.0

# MCP server
fastmcp>=2.0,<3

# Note: codanna for codebase context is not yet available on PyPI
# The system works without it but with limited codebase context

# NOTE: For torch, choose one:
# GPU (larger, ~2GB): pip install torch
# CPU-only (smaller): pip install torch --index-url https://download.pytorch.org/whl/cpu
