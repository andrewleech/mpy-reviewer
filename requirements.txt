# Core dependencies
requests>=2.28.0
anthropic>=0.25.0

# RAG system core
lancedb>=0.4.0
transformers>=4.36.0
# torch>=2.0.0  # See note below
click>=8.0.0
tqdm>=4.65.0
numpy>=1.24.0
pyarrow>=14.0.0

# Quality features (cross-encoder re-ranking, codebase context)
sentence-transformers>=2.2.0
codanna-cli>=0.1.0

# NOTE: For torch, choose one:
# GPU (larger, ~2GB): pip install torch
# CPU-only (smaller): pip install torch --index-url https://download.pytorch.org/whl/cpu
